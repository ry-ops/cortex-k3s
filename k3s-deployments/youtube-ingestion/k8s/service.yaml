apiVersion: v1
kind: Service
metadata:
  name: youtube-ingestion
  namespace: cortex
  labels:
    app: youtube-ingestion
spec:
  selector:
    app: youtube-ingestion
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
