apiVersion: v1
kind: Pod
metadata:
  name: conversation-test
  namespace: cortex-chat
spec:
  restartPolicy: Never
  containers:
  - name: test
    image: curlimages/curl:latest
    command: ["sleep", "3600"]
